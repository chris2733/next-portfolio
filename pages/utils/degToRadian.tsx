// degrees to radian calc - although this can be fixed later in the api to recieve measurements in radians instead
export default function degToRadian(num: number) {
  return num * (Math.PI / 180);
}
